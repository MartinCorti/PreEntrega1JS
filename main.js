calcular el valor de un producto en cuotas

primero precio de contado

luego precio en cuotas

    luego interés
    luego inflación
    finalmente cantidad de cuotas

si precio de contado < precio en cuotas => precio de contado
si precio de contado > precio en cuotas => precio en cuotas

// ENTRADA DE DATOS > PROCESAMIENTO DE DATOS > SALIDA (O INFORMACIÓN)
// Etapa 1 - Entrada de Datos
// Solicito el Monto de Contado

let monto = 0;

while ((monto <= 0))

// Realizamos los Cálculos
function calculoDePrecioCuotas(mon)